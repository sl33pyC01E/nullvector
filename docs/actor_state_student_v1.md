# Actor state student V1

This compact residual model distills the cellular actor-state transition away
from the rejected all-in-one editor. It predicts only causal organism state;
the successful Action-DiT remains responsible for world-frame latents. A
validation world selects checkpoint, raw/EMA state, gate threshold, and residual
magnitude before the untouched test world is evaluated once.
