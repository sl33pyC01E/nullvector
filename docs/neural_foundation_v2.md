# Neural foundation Build 3

Build 3 adds the recurrent action-frame path to the playable specialist ensemble. It binds the accepted recurrent Action-DiT, adapted continuous world decoder, frame compositor evaluation, and the previous playable foundation by exact file hashes.

The loaded desktop configuration contains 124.1M parameters. The physical scaffold remains authoritative while the recurrent frame model is evaluated over longer rollouts. The next model milestone is hidden-state distillation across the specialist ensemble, followed by a smaller composite and then mobile profiling.
