# Neural foundation build 1

The build-1 registry binds the specialist ensemble from organism morphology through city and biome dynamics. Every report and runtime artifact is hashed. Components are labeled `ready`, `validated_candidate`, or `experimental`; coverage is not confused with promotion readiness.

Build 1 is structurally complete when all required domains are present. Composite distillation starts only after experimental blockers are removed and metric-only candidates receive explicit replay gates.
